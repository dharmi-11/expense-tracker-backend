services:
  - type: web
    name: expense-tracker-backend
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install && npm run prisma:generate && npm run prisma:push && npm run build
    startCommand: npm run start:prod
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: DEMO_EMAIL
        value: demo@expensetracker.pro
      - key: DEMO_PASSWORD
        value: Passw0rd!2026
